meta {
  name: login_user
  type: http
  seq: 3
}

post {
  url: {{PROTOCOL}}://{{BASE_URL}}:{{PORT}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "yuriranjit@gmail.com",
      "password": "Password@123"
  }
}

script:post-response {
  bru.setEnvVar("refresh_token", res.body.refreshToken || res.body.newRefreshToken);bru.setEnvVar("access_token", res.body.accessToken || res.body.newAccessToken);
}

settings {
  encodeUrl: true
}
