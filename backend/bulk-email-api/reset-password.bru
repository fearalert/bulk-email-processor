meta {
  name: reset-password
  type: http
  seq: 11
}

post {
  url: {{PROTOCOL}}://{{BASE_URL}}:{{PORT}}/auth/reset-password
  body: json
  auth: inherit
}

body:json {
  {
      "token": "947258054925",
      "newPassword": "Password@123"
  }
}

settings {
  encodeUrl: true
}
