script:pre-request {
  bru.getEnvVar("refresh_token");
  bru.getEnvVar("access_token");
}

script:post-response {
  bru.setEnvVar("refresh_token", res.body.refreshToken);bru.setEnvVar("access_token", res.body.accessToken);
}
