meta {
  name: logout
  type: http
  seq: 8
}

post {
  url: {{PROTOCOL}}://{{BASE_URL}}:{{PORT}}/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "email": "dhakalrohanrd@gmail.com",
      "password": "Password@123"
  }
}

script:post-response {
  bru.setEnvVar("access_token", res.body.token || res.body.token);
}

settings {
  encodeUrl: true
}
